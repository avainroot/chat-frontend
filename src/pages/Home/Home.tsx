import {Page, TypographyTitle} from '@components';

export const PageHome = () => {
    return (
        <Page>
            <TypographyTitle level={'title'}>Активные чаты</TypographyTitle>
        </Page>
    )
}